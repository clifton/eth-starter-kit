{
  "extends": "solhint:recommended",
  "plugins": ["prettier"],
  "rules": {
    "quotes": "single",
    "compiler-version": ["warn", "^0.7.5"],
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "func-visibility": ["error", { "ignoreConstructors": true }]
  }
}
